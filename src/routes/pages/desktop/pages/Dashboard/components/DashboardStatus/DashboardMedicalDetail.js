import React from 'react';

const DashboardMedicalDetail = ({ medicalDetail }) => {
  // const { medical, medicalList } = medicalDetail;
  return <div>{JSON.stringify(medicalDetail)}</div>;
};

export default DashboardMedicalDetail;
